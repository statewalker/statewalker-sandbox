node_modules/
dist/
dist-browser/
*.tsbuildinfo
.turbo/
coverage/
# vitest browser mode: failure screenshots (attachments) from a red run.
.vitest/
.DS_Store
.env
.env.local
# httpeers-stack: generated signing keys, the hub's persisted state snapshot,
# and the invitation payload `pnpm bootstrap` writes (real secrets/derived data,
# not source).
.httpeers/
httpeers.json
