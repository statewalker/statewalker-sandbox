node_modules/
dist/
dist-browser/
*.tsbuildinfo
.turbo/
coverage/
.DS_Store
.env
.env.local
# httpeers-stack: generated signing keys, the hub's persisted state snapshot,
# and the invitation payload `pnpm setup` writes (real secrets/derived data,
# not source).
.httpeers/
httpeers.json
