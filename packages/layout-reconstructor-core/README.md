# @statewalker/layout-reconstructor-core

> **⚠ EXPERIMENTAL / INTERNAL** — evaluate. Private, non-published.

Experimental layout-reconstruction core. Uses `@lume/kiwi` Cassowary-style constraint solving to reconstruct 2D layouts from partial observations.

## TODO — evaluate

Currently isolated; no production consumer. Either promote to a real repo (if the approach pans out) or retire.
