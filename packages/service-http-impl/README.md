# @statewalker/service-http-impl

> **⚠ EXPERIMENTAL / INTERNAL** — scheduled for refactor. Private, non-published.

Hono-based implementation of `@statewalker/service-http`, with websocket support via `@hono/node-ws` and `@statewalker/service-rpc`.

## TODO — planned refactor

Follows whatever happens to `@statewalker/service-http`. Likely to be collapsed into the workbench's server shell.
