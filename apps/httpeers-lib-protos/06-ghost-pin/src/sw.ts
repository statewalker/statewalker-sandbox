/**
 * The worker: webrun-http-browser's dispatcher, and nothing else — the same
 * one line as `apps/httpeers-stack/src/pages/app/sw.ts`. The pin lives in the
 * page, not here.
 */
import "@statewalker/webrun-http-browser/sw-worker";
