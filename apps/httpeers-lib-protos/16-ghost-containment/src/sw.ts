/**
 * The worker: webrun-http-browser's dispatcher and nothing else — rung 06's,
 * unchanged. Containment is applied by the PAGE, on responses the ghost
 * already owns, which is the property that makes it deployable.
 */
import "@statewalker/webrun-http-browser/sw-worker";
