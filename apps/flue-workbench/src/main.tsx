/**
 * Placeholder entry. Replaced in task 9.x with the real workspace-picker
 * + xterm mount + createWorkbench wiring.
 */

const root = document.getElementById("root");
if (root) {
  root.textContent = "Flue Workbench — scaffolding placeholder. See README.draft.md.";
}
